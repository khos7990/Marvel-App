<!DOCTYPE html>
<html>
  <head>
    <title>Homepage</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1 class="title">The Marvelous Page</h1>
    <nav>
      <% if (user) { %>
          <a href="/logout">Log Out</a>
      <% } else { %>
          <a href="/auth/google">Login With Google</a>
      <% } %>
      </nav>
      <% if (user) { %>
              <h1> Welcome <%= user.name %> </h1>
          <% } %>
    
  </body>
</html>
